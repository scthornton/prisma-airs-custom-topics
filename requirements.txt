# Prisma AIRS API Management SDK
#
# The SDK is now GA on public PyPI. The old TestPyPI alpha pin
# (pan-airs-api-mgmt-sdk==0.0.1a14) is no longer needed.
#
# Requires Python 3.10 or newer.
# Install with: pip install -r requirements.txt
pan-airs-api-mgmt-sdk>=0.3.0
